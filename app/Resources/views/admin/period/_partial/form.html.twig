{% set edit = edit ?? false %}

<div class="errors">
    {{ form_errors(form) }}
</div>
<div class="row">
    <div class="col s12">
        {{ form_row(form.day_of_week) }}
    </div>
</div>
<div class="row">
    <div class="col s12 m6">
        {{ form_row(form.start) }}
    </div>
    <div class="col s12 m6">
        {{ form_row(form.end) }}
    </div>
</div>
<div class="row">
    <div class="col s12">
        {{ form_row(form.job) }}
    </div>
</div>

{% if edit %}
    {% include "admin/member/_partial/created_updated_card.html.twig" with { item: period } %}
{% endif %}
