{% extends 'layout.html.twig' %}

{% block breadcrumps %}
    <a href="{{ path('homepage') }}"><i class="material-icons">home</i></a> <i class="material-icons">chevron_right</i>
    <a href="{{ path('admin') }}"><i class="material-icons">tune</i>admin</a> <i class="material-icons">chevron_right</i>
    <i class="material-icons">add</i>Ajouter un créneau ponctuel
{% endblock %}

{% block content %}
    <h4 class="">Ajouter un créneau ponctuel</h4>
    {{ form_start(form) }}
    
    <div class="row">
        <div class="col s12">
            <div class="errors">
                {{ form_errors(form.date) }}
            </div>
            <div class="input-field">
                {{ form_label(form.date) }}
                {{ form_widget(form.date) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col s12">
            <div class="errors">
                {{ form_errors(form.start) }}
            </div>
            <div class="input-field">
                {{ form_label(form.start) }}
                {{ form_widget(form.start) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col s12">
            <div class="errors">
                {{ form_errors(form.end) }}
            </div>
            <div class="input-field">
                {{ form_label(form.end) }}
                {{ form_widget(form.end) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col s12">
            <div class="errors">
                {{ form_errors(form.formation) }}
            </div>
            <div class="select-field">
                {{ form_label(form.formation) }}
                {{ form_widget(form.formation) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col s12">
            <div class="errors">
                {{ form_errors(form.job) }}
            </div>
            <div class="select-field">
                {{ form_label(form.job) }}
                {{ form_widget(form.job) }}
            </div>
        </div>
    </div>

    <div>
        <button type="submit" class="btn-large waves-effect waves-light">Créer</button>
    </div>
    {{ form_end(form) }}
{% endblock %}
