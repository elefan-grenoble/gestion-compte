{% extends 'layout.html.twig' %}

{% block content %}
    <h4 class="">Nouveau code coffre à clefs extérieur</h4>
    {% if codes | length %}
    <div class="row">
        <div class="col s12">
            <div class="card-panel red">
                <span class="white-text">
                    <i class="material-icons">warning</i>
                    Aucun membre n'a confirmé avoir utilisé le dernier code pour récupérer les clefs. Es-tu certain de vouloir entrer un nouveau code ?
                </span>
            </div>
        </div>
    </div>
    {% endif %}
    {{ form_start(form) }}
    <div class="input-field">
        {{ form_widget(form.value) }}
        {{ form_label(form.value) }}
    </div>
    {{ form_widget(form) }}
    <div>
        <button type="submit" class="btn-large waves-effect waves-light">Créer un nouveau code</button>
    </div>
    {{ form_end(form) }}
{% endblock %}