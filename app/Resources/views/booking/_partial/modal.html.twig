<div id="book{{ bucket.firstBookable(app.user).id }}" class="modal">
    <form action="{{ path('shift_book',{'id':bucket.firstBookable(app.user).id}) }}" method="post">
        <div class="modal-content">
            <h4>Réserver ce créneau</h4>
            <h5>{{ bucket.start|date_fr_long }} de {{ bucket.start|date('G\\hi') }} à {{ bucket.end|date('G\\hi') }}</h5>
            {% if bucket.firstBookable(app.user).isDismissed %}
                <h6><i class="material-icons">star</i> Ceci est un créneau à reprendre</h6>
            {% endif %}
            {% if user.beneficiaries | length < 1 %}
                <p>Vous n'avez pas de bénéficiares</p>
            {% else %}
                <p><label>Nombre de places restantes: </label>{{ bucket.bookableShifts(app.user)|length }}/{{ bucket.shifts|length }}</p>
                <div class="row">
                    {% for shifter in 1..bucket.shifts|length  %}
                        {% if loop.index <= bucket.bookableShifts(app.user)|length %}
                            <div class="green lighten-1 left" style=" height:5px; width: {{ 100/(bucket.shifts|length) }}%"></div>
                        {% else %}
                            <div class="grey lighten-3 left" style="height:5px; width: {{ 100/(bucket.shifts|length) }}%"></div>
                        {% endif %}
                    {% endfor %}
                </div>
                <p>Attention ! Cette inscription vous engage. <br> Pour pouvoir annuler, votre créneaux devra trouver repreneur. </p>
                {% if user.beneficiaries | length > 1 %}
                    <div class="input-field">
                        <select name="beneficiaryId">
                            {% for beneficiary in user.beneficiaries %}
                                <option value="{{ beneficiary.id }}" data-icon="{{ gravatar(beneficiary.email) }}" class="left circle">{{ beneficiary.firstname }}</option>
                            {% endfor %}
                        </select>
                        <label>Choisir le bénéficiaire</label>
                    </div>
                {% else %}
                    <input type="hidden" name="beneficiaryId" value="{{ (user.beneficiaries|first).id }}">
                {% endif %}
            {% endif %}
        </div>
        <div class="modal-footer">
            <a href="#!"
               class="modal-action modal-close waves-effect waves-green btn-flat red-text ">Annuler</a>
            {% if user.beneficiaries | length >= 1 %}
                <button class="modal-action modal-close waves-effect waves-green btn teal">Confirmer</button>
            {% endif %}
        </div>
    </form>
</div>