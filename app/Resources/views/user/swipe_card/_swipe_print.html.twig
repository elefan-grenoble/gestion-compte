{% set code = card.code  %}
{% set encoded_code = code | vigenere_encode  %}
<td style="width: 70mm;height: 36mm; overflow: hidden;">
    <table style="width: 100%">
        <tbody style="width: 100%">
        <tr style="width: 100%">
            <td class="qr"><img src="{{ images_tmp_dir }}/{{ code }}_{{ 'qr.png' }}" alt="qr" /></td>
            <td class="bc" >
                <img src="{{ images_tmp_dir }}/{{ code }}_{{ 'bc.png' }}" alt="barecode">
                <br>
                Adhésion <strong>#{{ card.beneficiary.membership.memberNumber }}</strong> [B{{ card.number }}]
                <br>
                <strong>{{ project_url_display }}</strong>
            </td>
        </tr>
        <tr style="height: 6mm;width: 100%">
            <td colspan="2" class="info">
                <span>{{ card.beneficiary.firstname | capitalize }} {{ card.beneficiary.lastname | upper }}</span>
            </td>
        </tr>
        </tbody>
    </table>
</td>